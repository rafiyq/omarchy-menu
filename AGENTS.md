# AGENTS.md

Commands to run before committing changes:

```bash
make lint     # shellcheck static analysis
make test     # bats test suite
make fmt      # shfmt auto-formatting
make check    # lint + test
```

Fix all lint errors and test failures before committing.
